apiVersion: apps/v1
kind: Deployment
metadata:
  name: dspot-repo-monitor
  labels:
    app: dspot-repo-monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dspot-repo-monitor
  template:
    metadata:
      labels:
        app: dspot-repo-monitor
    spec:
      containers:
      - name: dspot-repo-monitor
        image: IMAGENAME
        ports:
          - containerPort: 30050
        env:
        - name: MONGODB_HOST
          value: mongodb://mongo:27017 # default if service of Mongodb deployed on K8s is called mongo with port 27017.
---
apiVersion: v1
kind: Service
metadata:
  name: dspot-repo-monitor
spec:
  ports:
  - port: 80
    targetPort: 30050
  selector:
    app: dspot-repo-monitor
  type: LoadBalancer
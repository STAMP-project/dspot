import spoon.Launcher;

public class Demo {

    private static String str = "str";

    private final String mess = "mess-Demo";

    private final String c = "global";

    private void mess(int x) {
        String mess = "mess-print";
        System.out.print(mess);
        String key = "local" + x;
        System.out.print(c + key);
    }

    private void test() {
        String mess = "mess-label";
        System.out.print(mess);
        ex: for (int value = 0; value < 10; value++) {
            node: for (int type = 10; type > 0; type--) {
                if (value != type) {
                    System.out.print("break as i" + value + "j" + type);
                    break ex;
                } else {
                    System.out.print("continue as i" + value + "j" + type);
                    continue node;
                }
            }
        }
    }

    private void exception() {
        try {
            throw Exception;
        } catch (Exception name) {
            name.printStackTrace();
        }
    }

    public static void main(String[] e) {
        System.out.print(Demo.str);
    }
}
